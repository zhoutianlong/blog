# jekyll_demo

##关于git的一些常用指令

1. git add . --添加所有修改

2. git commit -m "xxx" --为本次修改添加commit

3. git push --上传

4. git branch -r --查看远程分支

5. git branch -a --查看本地分支

6. git branch 分支名字 --创建分支

7. git checkout 分支名字 --切换分支